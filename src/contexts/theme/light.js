import { colors } from './colors';

export const light = {
  key: 'light',
  colors: {
    defaultText: colors.dark,
    highlightText: colors.highlight,
    defaultBackground: colors.light,
    highlightBackground: colors.highlight,
    coverBookBorder: colors.dark
  }
};
