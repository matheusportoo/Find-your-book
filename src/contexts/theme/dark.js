import { colors } from './colors';

export const dark = {
  key: 'dark',
  colors: {
    defaultText: colors.light,
    highlightText: colors.highlight,
    defaultBackground: colors.dark,
    highlightBackground: colors.highlight,
    coverBookBorder: colors.highlight
  }
};
